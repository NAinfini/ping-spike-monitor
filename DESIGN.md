# Ping Spike Monitor 设计系统

## 核心方向

界面是网络事件追踪工作台，不是 KPI 卡片墙。实时信号、链路位置、时间范围和证据必须保持同一条阅读路径：先判断是否异常，再定位发生在哪一层，最后打开原始样本核对。

## 布局与滚动

- 桌面使用 `236px + minmax(0, 1fr)` 双栏；侧栏固定为 `100dvh`，只有导航自身在内容超长时滚动。
- 主内容是页面的唯一纵向滚动所有者；切换视图回到顶部，初次加载不产生自动滚动。
- `960px` 以下改为顶部横向导航；应用最小窗口为 `720×560`，该宽度下五个导航项、链路带和页面均不得产生横向滚动条。
- 数据表、事件列表和设置表只有在内容确实超出自身边界时才拥有局部滚动。

## 视觉语言

- 字体：界面使用 Segoe UI Variable / Segoe UI；测量值与时间使用 Cascadia Code / Consolas。
- 标志：石墨圆角底承载冷青路径与单个琥珀诊断节点；只使用无文字图形版本，在标题栏、侧栏、托盘和安装包中共用同一资产。
- 形状：小半径、细边框、轻阴影；避免大面积圆角卡片、玻璃效果和装饰性渐变。
- 颜色：冷青色表示信号与选择，琥珀表示预热或警告，红色表示不可用，绿色仅表示有证据支持的健康状态。
- 状态不能只靠颜色；同时使用文字、图标、点形或线型。
- 明暗主题共享相同层级，ECharts 使用各自的文本、网格、基线和 tooltip 调色板。

## 图表规则

- 主线表示每个返回时间桶的最大 RTT；柱形表示丢包；虚线表示当前基线；事件使用背景区间。
- 不平滑曲线、不连接数据缺口、不伪造无数据时段。
- 坐标轴使用实际样本覆盖范围；首批样本不足一分钟时只扩展为一分钟可读窗口，并明确列出“请求窗口”和“实际覆盖”。
- 单个样本必须显示为点；短窗口刻度显示秒，较长窗口显示小时和分钟。
- 缩放仅使用图内交互和“重置缩放”，不显示容易被误认成滚动条的 slider。
- 图表必须启用 ARIA，并提供最近样本数据表作为等价入口。

## 组件与交互

- Base UI 负责 Button、Select、Dialog 等交互原语；所有控件保留键盘语义和清晰的 `:focus-visible`。
- 顶栏只放时间范围、自定义时间、语言、刷新和主题；低频诊断不冒充持续采样。
- 空状态必须说明缺少什么以及为何不能得出结论，绝不把“没有数据”显示成“健康”。
- 尊重 `prefers-reduced-motion`；过渡仅用于颜色、边框和轻微状态反馈。

## 国际化

- 首批语言：English、简体中文、Français。
- 所有日期、时间、数字、百分比和时长使用当前 locale 的 `Intl` 格式化。
- 语言选择写入本地存储，并同步更新 `<html lang>`。
- 新增文案必须同时补齐三份词典；最长语言在 `720px` 下不得造成页面或导航溢出。

## 禁止模式

- 不使用网络总分、仪表盘速度表、装饰性世界地图或没有证据的归因。
- 不在卡片中继续嵌套卡片，不用滚动条隐藏布局错误。
- 不展示虚构的区域节点、测速结果、路由快照或健康状态。
