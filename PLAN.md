# Ping Spike Monitor 实施计划

## 目标

构建一个 Windows 桌面应用，持续记录本地网络、ISP、公网基准点及不同地理区域的延迟、丢包和抖动，并以事件和图表解释一次卡顿最可能发生在哪一段链路。测速是低频、独立标记的诊断任务，不替代持续 Ping。

首版成功标准不是生成一个笼统“网络分数”，而是让用户在数秒内回答：

1. 当前是否异常？
2. 异常从路由器、ISP、公网还是特定区域开始？
3. 异常持续多久、影响哪些目标？
4. 测速负载是否显著抬高延迟？
5. 是否能导出足够清晰的证据交给 ISP？

## 首版范围

### 持续探测

| 层级 | 目标 | 默认频率 | 指标 |
| --- | --- | ---: | --- |
| 本地网络 | 自动发现的默认网关/路由器 | 2 秒 | RTT、超时、丢包、抖动 |
| 公网基准 | 两个不同提供商的稳定目标 | 2 秒 | RTT、超时、丢包、抖动 |
| ISP 上游 | 第一个可响应的 ISP 节点 | 5 秒 | RTT，仅作辅助证据 |
| 地理区域 | 每区至少两个固定单播目标 | 10 秒 | ICMP 或 TCP 延迟、丢包 |
| DNS | 指定解析器与测试域名 | 30 秒 | 解析时间与失败类型 |
| HTTPS | 固定 HTTPS 目标 | 30 秒 | 连接、TLS 与响应时间 |

默认区域为北美东部、北美西部、南美、欧洲、东亚和东南亚；大洋洲可选。Anycast 地址不能用于证明目标位于某一地区。区域必须使用可验证位置的固定单播节点，并保留每个节点的明细。

### 诊断任务

- `tracert`：网关正常而下游事件开始时运行，同一目标冷却 10 分钟。
- 互联网测速：手动或每 6 小时运行，记录下载、上传、空闲延迟和负载延迟。
- LAN/区域吞吐：通过已配置允许列表中的 `iperf3` 服务端手动运行。
- 测速期间所有 Ping 样本关联 `load_run_id`，用于分析 bufferbloat，但不触发普通故障事件。

### 明确不进入首版

- 云端账户、跨设备同步和云数据库。
- Docker 化本机采集器。
- 微服务和远程多租户平台。
- 可自由访问任意主机的测速代理。
- 自动通知、自动向 ISP 发信或自动修改网络配置。
- 世界地图式装饰性可视化。

## 技术方案

### 技术栈

- Electron：桌面窗口、托盘、应用生命周期和安装包。
- React + TypeScript + Vite：Renderer。
- shadcn/ui 的 Base UI 版本 + Tailwind CSS：组件与主题。
- TanStack Query：历史数据、事件、设置和测速结果的异步状态。
- Apache ECharts + `echarts-for-react`：图表；通过 `ReactEChartsCore` 按需引入。
- Electron IPC：Renderer 与主/采集进程通信。
- Node.js：调度、系统探测、命令执行与事件计算。
- `node:sqlite`：本地持久化，无 ORM。
- Node `node:test`：核心逻辑测试；Playwright：关键桌面流程和视觉检查。
- Ookla Speedtest CLI 与 `iperf3`：可选外部测速工具。

### 不采用的技术及原因

- Docker：会改变 Windows 主机网络路径，不能忠实观察真实网卡和默认网关。
- ORM：表数量有限且写入频繁，prepared statements 和编号 SQL migration 更直接。
- WebSocket：Electron IPC 已提供双向通信。
- Redux：服务器状态由 TanStack Query 管理，少量界面状态使用 React。
- 云数据库：断网时仍必须可靠写入，原始采样也不应依赖外部服务。
- 微服务：单机单用户应用没有独立部署或扩缩容需求。

### 进程边界

```text
Electron main
├── 窗口、托盘、单实例、休眠/恢复和应用生命周期
├── 安全 preload / IPC 白名单
│
├── Collector utility process
│   ├── topology / scheduler / probes
│   ├── health / incidents
│   └── SQLite
│
└── React renderer
    ├── shadcn/ui + Base UI
    ├── TanStack Query：查询和 mutation
    ├── IPC 实时样本
    └── echarts-for-react：可视化
```

Renderer 关闭或刷新不能停止采集；应用关闭窗口后保留在系统托盘。`nodeIntegration` 必须关闭，`contextIsolation` 必须开启，Renderer 只能使用 preload 暴露的窄接口。

## 采集与调度

- 调度使用单调时钟计算下一截止时间，数据库使用 UTC Unix 毫秒。
- 同一目标不允许重叠探测；超时后记录失败，不追赶补跑。
- 同一区域的两个节点同时采样，不同区域错开相位以降低瞬时进程数。
- 机器休眠、采集器重启、网络切换和拓扑变化写入 `system_events`，不伪装成丢包。
- 首版通过禁用 shell 的参数数组调用 `ping.exe`、`tracert.exe` 和测速工具。
- 24 小时测试若证明短进程创建是实际瓶颈，再将 ICMP 迁移到长期运行的 Windows worker；实现前不预先增加该复杂度。

## 健康基线、事件与归因

- 每个端点独立维护最近 6 小时健康样本的中位数和 MAD。
- 启动后至少预热 30 分钟；预热期间仅使用绝对阈值。
- 普通异常默认需要最近三次中两次异常；超时或极端 RTT 可立即开始事件。
- 连续健康 20–30 秒后结束事件；间隔不足 30 秒的异常合并。
- 两节点区域需要两者同时异常；三节点区域使用多数票且至少两个。
- 只有一个有效区域节点时显示“证据不足”，不能宣布整个区域异常。

归因规则：

- 网关异常：本地 Wi-Fi、网线或路由器方向。
- 网关正常，多个公网或区域同步异常：ISP 或公共上游方向。
- 仅一个区域满足 quorum：区域路由方向。
- 仅一个端点异常：端点问题。
- ISP 中间跳点不回应而终点正常：不判定故障。
- 所有归因均显示证据与置信程度，不把推测写成事实。

## 数据模型与保留

核心表：

- `targets`：名称、类型、区域、地址、探测方式、间隔、超时和启用状态。
- `samples`：计划时间、完成时间、目标、结果、RTT、错误码和测速关联。
- `rollups`：时间桶、成功数、丢包、min/avg/p50/p95/max 和抖动。
- `incidents`：范围、归因、严重度、起止时间和证据。
- `trace_runs` / `trace_hops`：事件路由快照。
- `speed_runs`：服务器、区域、下载、上传、空闲/负载延迟和状态。
- `system_events`：启动、停止、休眠、恢复和拓扑变化。

保留策略：

- 原始样本 30 天。
- 1 分钟聚合 90 天。
- 15 分钟聚合 1 年。
- 事件、路由和测速至少 1 年。
- 支持按时间、目标或事件导出 CSV。

数据库位于 `%LOCALAPPDATA%\PingSpikeMonitor`，不放入 OneDrive 仓库。SQLite 使用 WAL、prepared statements、批量事务和单写入进程。历史查询最多返回约 2,000 个时间桶，并保留 min/max 包络，避免聚合抹掉尖峰。

## 界面与图表

产品模式为 Operate：界面是“故障诊断工作台”，不是 KPI 卡片墙。

### 导航

- 概览
- 事件
- 区域
- 测速
- 目标与设置

### 概览首页

1. 顶栏显示采集状态、最后样本时间、时间范围和手动测速入口。
2. 链路健康带展示电脑 → 路由器 → ISP → 公网 → 区域。
3. 主时间序列展示 RTT、基线、丢包轨道、事件阴影和测速区间。
4. 区域矩阵展示各区域相对自身基线的恶化程度。
5. 最近事件展示持续时间、影响目标、证据和归因置信度。

### 图表

- 主图：RTT 折线与基线，默认最多四条线；丢包使用独立轨道。
- 区域：区域 × 时间热力图，并可下钻到固定节点。
- 事件：同步显示 RTT、抖动、丢包和路由快照。
- 测速：下载/上传趋势，以及空闲延迟与负载延迟哑铃图。
- 不使用仪表盘式速度表，不自动平滑，不连接数据缺口。

图表支持 15 分钟、1 小时、6 小时、24 小时、7 天、30 天和自定义范围；支持缩放、图例筛选、目标固定比较和事件下钻。查看历史时不强制跳回实时。

### 视觉与无障碍

- 安静、精确的仪器面板；中性背景、有限语义色、清晰排版和等宽数字。
- 支持亮色与暗色，不使用霓虹、过度玻璃化或持续闪烁。
- 状态同时使用文字、图标和线型，不能只依赖红绿颜色。
- ECharts 显式启用 ARIA，并提供表格或 CSV 替代。
- 支持键盘、可见焦点、减少动态效果和窄窗口布局。
- 首批提供 English、简体中文和 Français；语言选择持久化，日期、时间、数字和百分比使用对应 locale 格式化，新增语言只扩展词典与 locale 元数据。
- 桌面侧栏拥有独立的视口高度和滚动边界，不随主内容滚动；窄窗口切换为顶部横向导航。
- 图表坐标轴以实际样本覆盖范围为准，并同时标明请求窗口与实际覆盖；无数据时不绘制虚假的整段时间轴。

## 实施阶段

1. 写入本计划和产品事实，建立可运行 Electron/Vite 基础。
2. 实现统一样本、调度器、SQLite、模拟 Probe 与确定性事件测试。
3. 接入网关、公网、区域、DNS、HTTPS 与异常 `tracert`。
4. 实现概览、事件、区域、测速与设置界面。
5. 接入真实 Speedtest/iperf3、托盘、登录启动和安装包。
6. 完成桌面/窄窗口视觉 QA、数据恢复测试和 24 小时运行入口。

## 验收标准

- 关键严重异常在 2–6 秒内记录；区域严重异常在 10–20 秒内记录。
- 单一地区节点故障不误报整个区域。
- 网关、公网和单区域异常能被区分，并展示参与判断的证据。
- 休眠、重启和网络切换形成数据缺口或系统事件，不形成虚假丢包。
- 测速样本被标记且不触发普通故障事件。
- 30 天图表查询不超过约 2,000 个时间桶；实时显示延迟不超过 3 秒。
- 强制结束后数据库可恢复且 `integrity_check` 通过。
- 24 小时运行无重复采样、持续内存增长或失控子进程。
- Renderer 崩溃或窗口关闭时 Collector 继续运行。
- 键盘、对比度、数据表替代和响应式布局通过检查。
- 语言切换后主要视图、状态、图表与时间格式一致更新，刷新后保留选择，三种语言均不造成控件截断。
- 主页面滚动时桌面侧栏保持原位，1440×900 与应用最小窗口 720×560 均无页面级横向溢出或竞争滚动区。

## 外部前提

- 每个地区需要至少两个可信、固定且位置可验证的目标。
- LAN 吞吐需要支持 `iperf3` 的路由器或另一台有线设备。
- 区域吞吐需要受控并受防火墙保护的远端 `iperf3` 服务端。
- Windows 电脑休眠或关机时无法持续采集；真正无间断运行需要常开设备。
